1. Add new tests to `web/src/components/config-form/section-validations/__tests__/ffmpeg.test.ts` to cover the remaining uncovered lines in `ffmpeg.ts`.
2. The remaining uncovered lines handle edge cases for input and role formats, specifically ignoring inputs that are not objects, ignoring missing/invalid roles arrays, and ignoring role items that are not strings.
3. Verify test coverage is 100%.
4. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
